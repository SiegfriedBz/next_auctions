import linguiConfig from "../../lingui.config";

export const locales = linguiConfig.locales;
export const sourceLocale = linguiConfig.sourceLocale;
export const fallbackLocales = linguiConfig.fallbackLocales;
